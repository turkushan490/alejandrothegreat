services:
  alejandrothegreat:
    build: .
    image: alejandrothegreat:latest
    container_name: alejandrothegreat
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
